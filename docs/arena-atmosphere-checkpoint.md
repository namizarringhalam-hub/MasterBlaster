# Arena graphics and performance checkpoint

Request: apply the Apnea graphics lessons to Master Blaster (2026-09-25).

- Scope: coherent lighting/materials, distance-limited decorative detail, smaller shadow budgets, subtle shared environmental animation and local atmosphere. Keep gameplay geometry and combat readability intact.
- Baseline: clean working tree; existing WebGPU/WebGL renderer, instancing, pooled effects and half-resolution post effects retained. Native WebGPU high arena review at 1669×1300: 628 draws / 330,062 triangles, 359,269,218 reported texture bytes, no errors or fallback. Initial frame timings show browser throttling and are not a valid FPS benchmark.
- Validation planned: focused rendering/geometry checks, full npm test and build, native WebGPU and forced WebGL browser review, same-view renderer counters and 16-fighter stress.
- Implemented: warm key/cool restrained rim and softer environment fill; baked concrete floor and matte cable insulation; distance-culling of small decorative batches with bounding-radius margins and 8m hysteresis; 2048/1024/512 shadow tiers; four shared perimeter mesh shafts (two medium, zero low); shared arena-time reactor sweeps, honoring pause/reduced motion. PBR shader emission is isolated from reflected light in bloom.
- Regression found/fixed: detail registration initially recalculated conservative baked-cover bounds. Registration now retains existing bounds; original regression passes. A development hot reload briefly loaded the helper call before its definition was saved; fresh final-code startup is clean.
- Validated: full `npm test` exit 0 (27 Node checks plus 19 Worker tests), `npm run build` exit 0. Worker runner emitted sandbox log/static-analysis warnings, but both files/all 19 tests passed. Additional WGSL/GLSL emission regression passes after adding animated PBR coverage.
- Native WebGPU high, same arena view at 1669×1300: 601 draws / 327,782 triangles / 157,942,626 texture bytes; no errors/fallback. Low grazing and medium gameplay-camera views also render without errors/fallback. Inspected lossless arena and low grazing captures: cover outlines, route markers, surfaces and silhouettes remain readable.
- Native high 16-fighter stress: active impact rings/sparks, 265 draws / 181,334 triangles in the sampled gameplay view, no errors/fallback. This moving combat scene is not used for before/after counter comparisons.
- Forced WebGL high: WEBGL2 BLOOM, 600 draws / 327,781 triangles, 137,887,076 texture bytes, no errors/direct fallback. Inspected the lossless arena capture. Startup inspection timed out briefly; the loaded page subsequently responded and passed.
- Browser timing stays around 1000ms/frame (cause not isolated); do not claim a measured FPS improvement. Same-view native counters show 4.3% fewer draws and 56.0% less reported texture memory, not a GPU timing guarantee.
- Code review: collision/camera occluders and combat geometry untouched; existing conservative spheres preserved; bounded hysteresis, shared GPU clock, low-tier shaft removal and arena disposal covered by regression checks. No new dependencies.
- Implementation and validation complete. Release target: normal commit to origin/main; commit identity is recorded in Git history.

## Underwater skylight follow-up (2026-09-25)

- Request: light from the sky into the battlefield, evoking Apnea's underwater atmosphere.
- Implemented in `src/world.js`: blue-green overhead water glow with soft surface contours; four long slanting cyan shafts (two medium, zero low); slow world-space caustic contours on the floor and structural decks. Caustics modulate existing surface color and keep PBR lighting/shadows; no extra textures, lights or render passes. Arena clock preserves pause and reduced-motion behavior. Low/medium tiers reduce caustic contrast.
- Validated: full `npm test` exit 0 (27 Node checks and 19 Worker tests); production build/hosting check exit 0; explicit WGSL/GLSL shader regression passes with floor, deck, shaft and sky graphs. Tests cover quality transitions, caustic material assignment, non-emissive floor, unchanged collision and disposal.
- Browser: inspected native WebGPU high arena and low grazing lossless captures, plus forced WebGL high arena. Zero renderer errors or fallback. Same-view counts remain 601 native / 600 WebGL draws; reported texture memory unchanged from the previous version. Longer shafts increase pixel coverage and shader work; these counters are not an FPS guarantee.
- A WebGL control timeout resolved after closing the completed native preview. No product renderer errors were reported.
- Medium WebGL moving-combat check: 16-fighter stress running, arena unpaused, active impact rings/sparks, zero errors or fallback. Implementation and validation complete; release target is a normal commit/push to origin/main.

## Moody combat presentation (2026-09-25)

- User requested the first six suggested improvements, with implementation through completion: dark sky/scenery, readable combat/navigation, restrained architectural neon, layered explosions, reactive lighting, tighter selective bloom. District palette redesign is outside this request.
- Implemented dark sky/fog and reduced IBL/key light; narrow irregular shafts; subdued pillar seams with bright traversal edges; camera-facing team rim on existing armor material, preserving hit emission. Bloom selects stronger emission with tighter spread.
- Existing combat/projectile light pools strengthened without increasing their capacities. Explosions reuse bounded fire/smoke and add 32 hollow energy shells plus 32 ground-only fading scorch marks; weapon-coloured ribbons, initial flash, embers and darker smoke. Reduced motion limits travel, turbulence and brightness. Shader turbulence follows particle age rather than global time.
- Native high arena review: zero errors/fallback, idle draws remain 601; darker background and clear routes/players visually checked. Explosion review is being tuned for saturated colour; development review UI now offers fixed ages and reduced motion.
- Focused shader/effect tests passed before final tuning. Full suite found the increased muzzle intensity needed faster tail decay to meet the existing half-second expiry regression; decay adjusted. Next: validate staged effects, rerun affected/full checks and build, check native/WebGL combat and low-tier readability, commit and push request files.
- Root cause found during staged visual review: native WebGPU MRT defaults non-output attachments to NoBlending. Bloom was therefore ignoring particle transparency, making hollow shells glow as solid spheres. All bloom attachments now explicitly use MaterialBlending; regression covers high/medium WGSL/GLSL configurations.
- Final product code passes full `npm test` (27 Node checks and 19 Worker tests) and production build/hosting check. Expanded shell/scorch disposal assertions and focused shader/effect test also pass. Native staged captures at 180ms and 500ms show saturated hollow shells, asymmetric trails and clear silhouettes; reduced-motion capture shows smaller/dimmer effects with no ribbons. Low direct rendering also checked while isolating bloom.
- Native high 16-fighter combat: 16 sampled frames, zero errors/fallback. Forced WebGL high: inspected blue plasma shell/trails with transparency, local lighting and bloom intact; zero errors/fallback. Intermittent browser control timeouts recovered by opening a fresh WebGL-only page and allowing scene transitions to settle.
- Final medium WebGL 16-fighter combat: 15 sampled frames, zero errors/fallback. All six requested changes and validation complete. No FPS claim: browser timing remains unsuitable for benchmarking. Release via normal commit/push to origin/main; no outstanding implementation steps.

## Reference night city and moving mecha back glow (2026-09-25)

- Request: match the supplied photograph's dark, hazy, clouded cyberpunk city atmosphere and make moving mechas stand out with rear neon.
- Implemented layered procedural low clouds with cyan/magenta city light diffusion, height/distance fog, four soft external horizon mist banks replacing the overhead beams, 80 varied-height skyline buildings with irregular emissive windows. Removed the floating horizon ring. No image assets or new shadow lights.
- Rear mecha vents and feathered halos use two merged geometry layers per fighter, face backwards, and retain depth testing. Actual horizontal/vertical speed drives a smoothed intensity without flashing; stopping dims them. Death/respawn reset brightness. Existing thruster geometry remains intact.
- Focused tests pass: WGSL/GLSL compilation for fog, skyline windows, mist, both rear glow materials; world cleanup restores prior fog; motion/stop/vertical/death/respawn and material disposal. Native high arena lossless capture inspected: detailed dark skyline and cloud/haze layers, 619 draws in review view, no renderer errors/fallback.
- Final validation: full `npm test` exit 0 (27 Node checks and 19 Worker tests); production build/hosting check exit 0; `git diff --check` passed. Existing Worker sandbox/static-analysis warnings and build chunk warning remain.
- Inspected native running rear view: two cyan strips and soft halos sit on the backpack pods. Low direct gameplay capture keeps the fighter, footing and route markers readable. Inspected WebGL high city/sky capture; WebGPU high and WebGL high 16-fighter stress runs both report zero errors and no direct fallback.
- A staged gait review raised the harness's route-obstructed assertion; a fresh default-pose combat fixture passed. WebGL startup took about 47 seconds and temporarily timed out browser controls, then rendered successfully. No frame-rate claim: this environment remains unsuitable for timing benchmarks.
- Implementation and validation complete; next step is normal commit/push of the six request files to origin/main. No outstanding product changes.
